// Placeholder — full Profile lands in commit 8.
import { BottomNav } from '../components/BottomNav'
export function ProfileScreen() {
  return (
    <div style={{ minHeight: '100dvh', padding: 40 }}>
      ProfileScreen · /walk/profile (placeholder)
      <BottomNav />
    </div>
  )
}
