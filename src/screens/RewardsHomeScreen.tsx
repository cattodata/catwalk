// Placeholder — full tier dashboard lands in commit 7.
import { BottomNav } from '../components/BottomNav'
export function RewardsHomeScreen() {
  return (
    <div style={{ minHeight: '100dvh', padding: 40 }}>
      RewardsHomeScreen · /walk/rewards (placeholder)
      <BottomNav />
    </div>
  )
}
